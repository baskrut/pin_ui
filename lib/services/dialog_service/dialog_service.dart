class DialogService{
  DialogService._privateConstructor();
  static final DialogService _instance = DialogService._privateConstructor();
  static DialogService get instance => _instance;
  Future show(Future future, ){

    return future;
  }
}